name: validate-reconstruction

on:
  push:
    branches: [main, rebuild-live-renderer]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Validate reconstructed renderer
        run: npm run check

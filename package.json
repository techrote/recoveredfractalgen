{
  "name": "recovered-fractal-generator",
  "version": "1.0.0-reconstruction",
  "private": true,
  "description": "Dependency-free WebGL2 reconstruction of a recovered live fractal renderer",
  "scripts": {
    "check": "node --check src/app.js && node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
